extends layout

block content
  .container.main-content
    if (!user)
      h1 Welcome
      p Please login to enter the fitness app
    if (user)
      h1 Welcome to fitness app
      br
      p You are currently logged in as #{user.username}
